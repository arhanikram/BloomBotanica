<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Background color for the entire BottomNavigationView -->
    <solid android:color="@color/darkgreen" />

    <!-- Rounded corners -->
    <corners android:radius="64dp" />

    <!-- Padding to create spacing inside the BottomNavigationView -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />
</shape>
